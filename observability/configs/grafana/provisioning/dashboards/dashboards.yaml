apiVersion: 1

providers:
  - name: Gateway
    orgId: 1
    folder: Gateway
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
